<h1>Items</h1>
<table>
  <th>ID</th>
  <th>URL</th>
  <th>Target Price</th>
  <% @items.each do |item| %>
    <tr>
      <td><%= item.id %></td>
      <td><%= item.url %></td>
      <td><%= item.target_price %></td>
    </tr>
  <% end %>
</table>

<%= link_to "Add new item", new_item_path %>